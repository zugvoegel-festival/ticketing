# ticketing — planning pointer

Long-form architecture and ops docs live under **`docs/`** (source of truth). This file exists so vibe/MCP workflows that expect `.vibe/development-plan.md` still resolve.

- **`docs/ARCHITECTURE.md`** — flake, modules, where to change config, invariants
- **`docs/BACKUP.md`** — restic jobs, secrets keys, restore/add-service flow
- **`AGENTS.md`** — short command list for agents

Track exploratory tasks in your issue tracker or chat; avoid duplicating a second architecture copy here.
