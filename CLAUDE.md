# ticketing — AI context

Use **`AGENTS.md`** for commands and invariants, **`modules/README.md`** for the module map, and **`.vibe/docs/`** for cross-module architecture. No external MCP workflow is required for task tracking.
